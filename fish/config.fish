# ── Fish Configuration ─────────────────────────────────────────────────
#
# Structure:
#   conf.d/*.fish   — auto-sourced on shell startup (env, tools, abbr, etc.)
#   functions/*.fish — auto-loaded on first invocation (custom functions)
#   .env            — secrets (gitignored), sourced by conf.d/env.fish
#
# See each file for details.
